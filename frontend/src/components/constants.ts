const SERVER_URL = 'http://localhost:5001/api';

const GROUPS = {
  CONTACTS: 'contacts',
  USERS: 'users',
};

const USER_ACTIONS = {
  REGISTER: 'register',
  LOGIN: 'login',
  LOGOUT: 'logout',
  PROFILE: 'profile',
};

export { SERVER_URL, GROUPS, USER_ACTIONS };